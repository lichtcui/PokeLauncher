# 在鸿蒙（HarmonyOS NEXT）上以原生 HAP 运行 PokeRogue —— 技术调研与实现方案

> 目标：做一个**原生 HAP 应用**，在鸿蒙手机上直接运行 PokeRogue，摆脱卓易通 + Android APK 的兼容层。
> 结论先说：**核心完全可行，本质是「ArkWeb 内嵌浏览器 + 离线资源本地代理」**，难度集中在 ArkWeb 内核兼容性和 HAP 签名/分发，而不是玩法逻辑本身。

---

## 1. 参考实现原理拆解

### 1.1 Electron 版（`Admiral-Billy/Pokerogue-App`）

| 环节 | 实现 |
| --- | --- |
| 运行环境 | Electron `BrowserWindow`（Chromium） |
| 在线模式 | `loadURL('https://pokerogue.net/')` |
| 离线模式 | `loadFile(gameDir + '/index.html')`（即 `file://`） |
| 离线资源 | 从 GitHub Release 下载 `game.zip`（`Admiral-Billy/pokerogue`），解压到本地 `game/` 目录 |
| 存档 | `localStorage`（游戏内加密后存 `data_<user>`、`sessionData_<user>`、`settings` 等）+ Electron 的 Local Storage |
| 版本记录 | `game/currentVersion.txt` 写入 Release tag |
| 其它 | 注入 CSS 改背景/隐藏光标、下载 blob 存档、快捷键等 |

关键源码：`src/file_tab.js`（下载解压）、`src/utils.js`（`downloadFile`/`resetGame`）、`src/main.js`（`loadFile`）。

### 1.2 Android 版（`Admiral-Billy/PokeRogue-App-Android`）

与 Electron 几乎一一对应，但有两点关键差异：

| 环节 | 实现 |
| --- | --- |
| 运行环境 | `WebView`（`javaScriptEnabled` + `domStorageEnabled` + `allowFileAccess`） |
| **离线资源服务** | **内嵌 NanoHTTPD 本地服务器，监听 `http://localhost:8000`**，把 `getExternalFilesDir()/game` 当根目录返回文件；WebView 加载 `http://localhost:8000/index.html` |
| 下载 | `HttpURLConnection` 下载 `game.zip` → `ZipFile` 解压 → 删 zip |
| 存档 | WebView 的 `localStorage` |
| 存档导入导出 | `DownloadListener` 拦截 `blob:` → JS 读成 base64 → `AndroidInterface.downloadFile` 写 Downloads；`onShowFileChooser` 做导入 |
| 适配 | 注入 JS 对 `#app` 做 `transform: scale()` 适配屏幕；手柄方向键映射；前后台 `pauseBgm/resumeBgm` |

**为什么 Android 不用 `file://`？** 因为 Vite 产物用 ES Module + 运行时 `fetch`，`file://` 下会被 CORS 拦截、模块脚本无法加载。本地 HTTP 服务器给了一个正常的 `http://localhost` origin，`localStorage`/`fetch`/模块都正常。

### 1.3 离线包到底是什么

- 上游仓库 `pagefaultgames/pokerogue` 用 `pnpm build:app` 构建（`vite build --mode app`）。
- `.env.app`：`VITE_BYPASS_LOGIN=1`、`VITE_SERVER_URL=http://localhost:8001` ⇒ **离线包本身就绕过了登录、不依赖真实服务器，存档走 localStorage**。
- `vite.config.ts`：`base: ""` ⇒ 产物用相对路径 `./assets/...`。
- 当前 Release 资产（`Admiral-Billy/pokerogue` latest，tag `release-e4e9b53`）：
  - `game.zip` ≈ **531 MB**
  - `game_beta.zip` ≈ 528 MB
  - `game_futaba_mod.zip` ≈ **302 MB**（用户目前用的 PodarSmarty 版对应这个 mod）
  - `kanto_version.zip` ≈ 507 MB
- `game.zip` 内含 `index.html`、`index.css`、`assets/*.js`、`images/`、`audio/`、`fonts/`、`locales/`、`service-worker.js`、`currentVersion.txt` 等。

> 注意：`index.html` 里有 `navigator.serviceWorker.register('./service-worker.js')`，但被 `if ("serviceWorker" in navigator)` 包裹且失败会被 catch，不阻塞运行。ArkWeb 若不支持 SW 也不会影响。

---

## 2. 鸿蒙侧技术选型与可行性

### 2.1 结论表

| 需求 | 鸿蒙能力 | 结论 |
| --- | --- | --- |
| 内嵌运行网页游戏 | `Web` 组件（ArkWeb，Chromium 内核） | ✅ 可行，Phaser/WebGL/Canvas 由内核支持 |
| 替代本地 HTTP 服务器 | `Web.onInterceptRequest()` + `WebResourceResponse.setResponseData(fd)` | ✅ **推荐**，无需真起服务器 |
| 离线包下载 | `@ohos.request` `downloadFile`（带进度/后台）或 `@ohos.net.http` | ✅ |
| 解压 | `@ohos.zlib` `decompressFile` | ✅ 原生 zip 解压 |
| 存档持久化 | `.domStorageAccess(true)`（localStorage） | ✅ 游戏只依赖 localStorage |
| 存档导入/导出 | `WebDownloadDelegate`（捕获 blob 下载）+ `onShowFileSelector` | ✅ |
| 屏幕适配/触控 | 游戏自带 `#touchControls` 触控层；必要时注入 scale JS | ✅ |
| 安装 | HAP 签名 + `hdc install` / 开发者模式 | ⚠️ 见第 6 节，是主要工程外成本 |

### 2.2 为什么用 `onInterceptRequest` 而不是 `file://` 或真服务器

- `file://`：ES Module、`fetch('./locales/...')` 会被 CORS 拦，`base: ""` 也救不了模块脚本，**不可行**。
- 真起 HTTP 服务器：鸿蒙没有现成的 `NanoHTTPD` 级别库，需基于 `@ohos.net.socket` 手写 HTTP 解析，工作量和坑都更多。
- **`onInterceptRequest`（推荐）**：把 `https://pokerogue.local/...` 的请求全部截获，从沙箱文件读取并构造响应。
  - 得到一个**正常的 https origin**（安全上下文），localStorage 可持久化；
  - 通过 `setResponseData(fd)` 直接传文件描述符，**不用把整个文件读进内存**，对几百 MB 资源友好；
  - 支持异步：先 `setResponseIsReady(false)`，读完后 `setResponseIsReady(true)`；
  - 官方文档明确「可拦截重定向请求并返回响应数据」，官方还有示例 `gitee.com/harmonyos_samples/web-interceptor`。

> 备选：`WebSchemeHandler` + `customizeSchemes('pokerogue')` 自定义 scheme。它能拿到 POST body、支持流式响应，但自定义 scheme 的 storage origin 不保证持久化，且需要先注册 scheme。**首版用 https 伪域名方案更稳。**

---

## 3. 总体架构

```
┌──────────────────────────────────────────────────────────┐
│                    HarmonyOS HAP (ArkTS)                 │
│                                                          │
│  IndexPage (启动器)                                       │
│    ├─ 检查本地游戏文件 / 版本                              │
│    ├─ [下载/更新]  ──► GameRepository.download()          │
│    │                      request.downloadFile → zip      │
│    ├─ [删除文件]   ──► GameRepository.clear()             │
│    ├─ [离线开始]   ──► router → GamePage                   │
│    └─ [在线开始]   ──► Web(src: https://pokerogue.net)     │
│                                                          │
│  GamePage (Web 游戏页)                                    │
│    ├─ Web({ src: 'https://pokerogue.local/index.html' })  │
│    │     .domStorageAccess(true).fileAccess(true)...      │
│    │     .onInterceptRequest(LocalContentProvider)        │
│    └─ 全屏 / 横竖屏 / 返回拦截 / 存档导入导出              │
│                                                          │
│  LocalContentProvider                                     │
│    URL https://pokerogue.local/<path>                     │
│      → filesDir/game/<path>                               │
│      → fileIo.openSync → WebResourceResponse.setResponseData(fd) │
│                                                          │
│  存储： <sandbox>/files/game/  (解压后的离线包)            │
└──────────────────────────────────────────────────────────┘
```

---

## 4. 模块设计与关键代码

### 4.1 工程结构

```
harmony-pokerouge/
├─ AppScope/app.json5
├─ entry/
│  ├─ src/main/
│  │  ├─ module.json5                       # INTERNET 权限
│  │  ├─ ets/
│  │  │  ├─ entryability/EntryAbility.ets
│  │  │  ├─ pages/Index.ets                 # 启动器
│  │  │  ├─ pages/GamePage.ets              # Web 游戏页
│  │  │  ├─ common/Const.ets                # URL/路径常量
│  │  │  ├─ common/Mime.ets                 # 扩展名 → MIME
│  │  │  ├─ model/GameRepository.ets        # 下载/解压/删除/版本
│  │  │  └─ model/LocalContentProvider.ets  # onInterceptRequest 实现
│  │  └─ resources/
│  │     └─ base/element/string.json ...    # 仅应用自身资源，不含游戏资源包
│  ├─ build-profile.json5
│  └─ oh-package.json5
├─ build-profile.json5
└─ hvigorfile.ts
```

### 4.2 `module.json5`：权限

```json5
{
  "module": {
    "name": "entry",
    "type": "entry",
    "requestPermissions": [
      { "name": "ohos.permission.INTERNET" }
    ]
    // ...
  }
}
```

### 4.3 常量

```ts
// common/Const.ets
export const GAME_ORIGIN = 'https://pokerogue.local';
export const GAME_START_URL = GAME_ORIGIN + '/index.html';
export const RELEASE_API = 'https://api.github.com/repos/Admiral-Billy/pokerogue/releases/latest';
export const USER_AGENT = 'Pokerogue-Harmony';
// 可选资产名：'game.zip' | 'game_futaba_mod.zip' | 'game_beta.zip' | 'kanto_version.zip'
export const DEFAULT_ASSET = 'game.zip';
```

### 4.4 下载 + 解压（`GameRepository`）

```ts
// model/GameRepository.ets
import { request, zlib, BusinessError } from '@kit.BasicServicesKit';
import { http } from '@kit.NetworkKit';
import { fileIo as fs } from '@kit.CoreFileKit';
import { common } from '@kit.AbilityKit';
import { RELEASE_API, USER_AGENT, DEFAULT_ASSET } from '../common/Const';

export interface ReleaseInfo { tag: string; assetName: string; assetUrl: string; assetSize: number; }

export class GameRepository {
  constructor(private ctx: common.UIAbilityContext) {}

  get gameDir(): string { return this.ctx.filesDir + '/game'; }
  get zipPath(): string { return this.ctx.cacheDir + '/game.zip'; }
  get versionFile(): string { return this.gameDir + '/currentVersion.txt'; }

  hasGameFiles(): boolean {
    try {
      const names = fs.listFileSync(this.gameDir);
      return names.indexOf('index.html') >= 0;
    } catch (_) { return false; }
  }

  /** 查询 GitHub Release 里指定资产的下载地址 */
  async fetchRelease(assetName: string = DEFAULT_ASSET): Promise<ReleaseInfo> {
    const httpRequest = http.createHttp();
    try {
      const resp = await httpRequest.request(RELEASE_API, {
        method: http.RequestMethod.GET,
        header: { 'User-Agent': USER_AGENT, 'Accept': 'application/vnd.github+json' }
      });
      const data = JSON.parse(resp.result as string);
      const asset = (data.assets as Array<{ name: string, browser_download_url: string, size: number }>)
        .find(a => a.name === assetName);
      if (!asset) { throw new Error(`asset ${assetName} not found`); }
      return { tag: data.tag_name, assetName, assetUrl: asset.browser_download_url, assetSize: asset.size };
    } finally {
      httpRequest.destroy();
    }
  }

  /** 下载离线包（带进度），完成后解压 */
  async downloadAndExtract(
    info: ReleaseInfo,
    onProgress: (received: number, total: number) => void
  ): Promise<void> {
    // 1) 下载
    const task = await request.downloadFile(this.ctx, {
      url: info.assetUrl,
      filePath: this.zipPath,
      enableMetered: true,
      enableRoaming: true,
      background: true
    });
    await new Promise<void>((resolve, reject) => {
      task.on('progress', (r: number, t: number) => onProgress(r, t));
      task.on('complete', () => resolve());
      task.on('fail', (err: BusinessError) => reject(new Error(JSON.stringify(err))));
    });

    // 2) 清理旧文件并解压（outFile 目录必须已存在）
    fs.rmdirSync(this.gameDir);                 // 递归删除，旧目录不存在会抛错，用 try 包一下
    fs.mkdirSync(this.gameDir, true);
    await zlib.decompressFile(this.zipPath, this.gameDir, {
      parallel: zlib.ParallelStrategy.PARALLEL_STRATEGY_PARALLEL_DECOMPRESSION
    });

    // 3) 清理 zip + 记录版本
    fs.unlinkSync(this.zipPath);
    fs.writeFileSync(this.versionFile, info.tag);
  }

  clear(): void {
    try { fs.rmdirSync(this.gameDir); } catch (_) {}
  }
}
```

> 注意点：
> - `fs.rmdirSync` 递归删除前用 `try/catch`，目录不存在会抛错。
> - `request.downloadFile` 的 `filePath` 建议放 `cacheDir`；解压目标必须在沙箱内且已创建。
> - 531 MB 下载 + 解压可能耗时数分钟，UI 必须显示进度并防止息屏（可申请 `ohos.permission.KEEP_BACKGROUND_RUNNING` 或用后台传输 `request.agent`）。

### 4.5 本地资源代理（`LocalContentProvider`）

```ts
// model/LocalContentProvider.ets
import { webview } from '@kit.ArkWeb';
import { fileIo as fs } from '@kit.CoreFileKit';
import { GAME_ORIGIN } from '../common/Const';
import { mimeOf } from '../common/Mime';

export class LocalContentProvider {
  constructor(private gameDir: string) {}

  handle(event: OnInterceptRequestEvent): WebResourceResponse | null {
    const url = event.request.getRequestUrl();
    if (!url.startsWith(GAME_ORIGIN)) {
      return null; // 其它域名（真实网络）放行
    }

    let rel = url.substring(GAME_ORIGIN.length);
    rel = rel.split('#')[0].split('?')[0];
    rel = decodeURIComponent(rel);
    if (rel === '' || rel === '/') { rel = '/index.html'; }

    // 防目录穿越
    const filePath = this.gameDir + rel;
    const response = new WebResourceResponse();
    try {
      const file = fs.openSync(filePath, fs.OpenMode.READ_ONLY);
      // 传 fd：由 Web 组件负责关闭，避免大文件进内存
      response.setResponseData(file.fd);
      response.setResponseMimeType(mimeOf(filePath));
      response.setResponseEncoding('utf-8');
      response.setResponseCode(200);
      response.setReasonMessage('OK');
      return response;
    } catch (_) {
      response.setResponseCode(404);
      response.setReasonMessage('Not Found');
      response.setResponseMimeType('text/plain');
      response.setResponseData('404');
      return response;
    }
  }
}
```

### 4.6 MIME 映射（关键！否则 ES Module 会加载失败）

Chromium 对 `<script type="module">` 有严格的 MIME 校验，返回错误 MIME 会报
`Failed to load module script: Expected a JavaScript module script but the server responded with a MIME type of ...`。

```ts
// common/Mime.ets
const MAP: Record<string, string> = {
  html: 'text/html', htm: 'text/html',
  js: 'text/javascript', mjs: 'text/javascript',
  css: 'text/css',
  json: 'application/json',
  webmanifest: 'application/manifest+json',
  wasm: 'application/wasm',
  png: 'image/png', jpg: 'image/jpeg', jpeg: 'image/jpeg', gif: 'image/gif',
  webp: 'image/webp', svg: 'image/svg+xml', ico: 'image/x-icon',
  mp3: 'audio/mpeg', ogg: 'audio/ogg', m4a: 'audio/mp4', wav: 'audio/wav',
  ttf: 'font/ttf', otf: 'font/otf', woff: 'font/woff', woff2: 'font/woff2',
  txt: 'text/plain', xml: 'text/xml'
};

export function mimeOf(path: string): string {
  const dot = path.lastIndexOf('.');
  if (dot < 0) { return 'application/octet-stream'; }
  const ext = path.substring(dot + 1).toLowerCase();
  return MAP[ext] ?? 'application/octet-stream';
}
```

### 4.7 游戏页（`GamePage`）

```ts
// pages/GamePage.ets
import { webview } from '@kit.ArkWeb';
import { common } from '@kit.AbilityKit';
import { LocalContentProvider } from '../model/LocalContentProvider';
import { GAME_START_URL } from '../common/Const';

@Entry
@Component
struct GamePage {
  controller: webview.WebviewController = new webview.WebviewController();
  private provider?: LocalContentProvider;

  aboutToAppear(): void {
    const ctx = this.getUIContext().getHostContext() as common.UIAbilityContext;
    this.provider = new LocalContentProvider(ctx.filesDir + '/game');
  }

  build() {
    Column() {
      Web({ src: GAME_START_URL, controller: this.controller })
        .domStorageAccess(true)        // localStorage 持久化（存档）
        .fileAccess(true)              // 文件选择/下载
        .javaScriptAccess(true)
        .imageAccess(true)
        .onlineImageAccess(true)
        .zoomAccess(false)
        .onInterceptRequest((event) => this.provider?.handle(event) ?? null)
        .onShowFileSelector((event) => {
          // 存档导入：拉起系统文件选择器，把选中文件交给 event.result
          return true;
        })
        .width('100%')
        .height('100%')
    }
    .width('100%')
    .height('100%')
  }
}
```

适配补充：
- 游戏自带触控层 `#touchControls`，默认适配移动端；若比例不对，可参照 Android 版注入 `transform: scale(min(w/gw, h/gh))`。
- 横屏体验更好，可在 `EntryAbility.onWindowStageCreate` 里用 `window.setPreferredOrientation(Orientation.LANDSCAPE)`。
- 返回键用 `onBackPressed` 拦截，避免误退出。

### 4.8 启动器（`Index`）功能清单

1. 展示本地游戏状态（未安装 / 已安装 / 版本号）。
2. **下载/更新**：查询 Release → 选资产（默认 `game.zip`，可切 futaba mod）→ 进度条 → 解压 → 完成。
3. **删除文件**：清空 `files/game`。
4. **离线开始**：跳转 `GamePage`。
5. **在线开始**（可选）：`Web(src: 'https://pokerogue.net')`。

---

## 5. 里程碑计划

| 阶段 | 目标 | 验收 |
| --- | --- | --- |
| **P0 内核验证（最高优先级）** | 写一个最小 HAP：`Web` 组件直接加载 `https://pokerogue.net`，开启 `domStorageAccess` | 能进游戏主界面、能开始战斗、音频/画面正常 ⇒ 证明 ArkWeb 能跑 PokeRogue |
| **P1 离线闭环** | 下载 `game.zip`（可先用 `game_futaba_mod.zip` 302MB 缩短验证）→ zlib 解压 → `onInterceptRequest` 代理 → 加载 `https://pokerogue.local/index.html` | 断网能进游戏、能存档、重启后存档还在 |
| **P2 体验完善** | 全屏/横屏、进度 UI、删除/更新、存档导入导出、后台暂停 BGM、断点续传/失败重试 | 日常可用的完整 App |
| **P3 工程化（可选）** | 应用图标/名称、签名打包、GitHub Actions 自动构建 HAP、镜像下载源 | 可分发 |

**强烈建议先做 P0**：这是唯一可能推翻整个方案的点（若 ArkWeb 跑不动 Phaser/WebGL，后面都白做）。

---

## 6. 风险与对策

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| **ArkWeb 兼容性** | Phaser 3 + WebGL/Canvas + Web Audio 在 ArkWeb 上可能有差异（音频自动播放、纹理、性能） | P0 先验证；音频用 `.onAudioResume`/用户手势触发；必要时降级 Canvas 渲染 |
| **HAP 签名与安装** | HarmonyOS NEXT 对第三方 HAP 安装限制严格（纯净模式），需签名 + 设备授权 | 本项目走第三方 HAP 安装站（重签/共享证书）分发，基本绕过该限制（见 7.4）；自测用 DevEco 自动签名或 `hdc install` debug 包 |
| **531 MB 下载** | GitHub 直连在国内可能慢/失败 | 支持自定义镜像/代理 URL；用后台传输 `request.agent` 断点续传；提供 futaba mod（302MB）作为默认精简选项 |
| **解压耗时/磁盘** | 数万小文件，解压可能几分钟，占用 ~1GB | 显示进度、防息屏、`parallel` 并行解压；空间不足提前检测 |
| **`onInterceptRequest` 性能** | 回调在主线程，每资源一次同步 `openSync` | 只 `openSync` 拿 fd（廉价），不读内容；必要时用 `WebSchemeHandler` 异步流式响应 |
| **MIME 错误** | 模块脚本 MIME 不对直接白屏 | 用 4.6 的 MIME 映射，`.js` 必须是 `text/javascript` |
| **Service Worker** | `service-worker.js` 可能注册成功并缓存旧资源 | 可返回 404 让注册失败；或接受其行为 |
| **存档 origin 绑定** | localStorage 绑定 `https://pokerogue.local`，换域名会丢档 | 域名固定，不要改；升级只换文件不动 origin |
| **目录穿越** | 恶意 URL `../` 读沙箱外文件 | 规范化路径并校验必须以 `gameDir` 开头 |

---

## 7. 资源来源与加载方式

### 7.1 资源来源：运行时下载（唯一方案）

本项目**只采用「运行时下载资源包」**，不把游戏资源打进 HAP 安装包。

- HAP 里只有代码（体积几 MB）。
- 首次启动后从 GitHub Release 下载 `game.zip`，解压到应用沙箱 `filesDir/game`。
- 之后每次启动直接跑沙箱里的文件；游戏更新时只重新下载资源包，无需重装 HAP。
- 与现有 Electron / Android 实现保持一致。

> `game.zip` 里不含可执行文件，只是网页资源（`index.html`、`assets/`、`images/`、`audio/` 等），最终交给 Web 组件加载。

### 7.2 加载方式对比

| 方案 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| **A. `onInterceptRequest` + 伪 https 域名** | 官方能力、无需起服务、storage 正常、大文件用 fd | 主线程回调、不能拿 POST body | ✅ **首选** |
| B. `WebSchemeHandler` + 自定义 scheme | 异步流式、可拿 body | 自定义 scheme 的 storage 持久化存疑、需注册 scheme | P1 后如遇性能问题再换 |
| C. 自实现本地 HTTP 服务（`net.socket`） | 最接近 Android 方案、origin 为 `http://localhost` | 要手写 HTTP 解析、并发/性能坑多 | 备选 |
| D. `file://` 直接加载沙箱文件 | 最简单 | ES Module/fetch 被 CORS 拦，白屏 | ❌ 不可行 |

### 7.3 安装方式

本项目通过第三方 HAP 安装站（如 `sydxky.cn/install-tools.php` 这类重签/共享证书分发）安装，**可绕过 HarmonyOS NEXT 的官方签名与纯净模式限制**，因此第 6 节里「HAP 签名/设备 UDID」的工程外成本大幅降低。自测阶段用 DevEco Studio 自动签名或 `hdc install` 装 debug 包即可。

---

## 8. 参考资料

- 参考实现
  - Electron: https://github.com/Admiral-Billy/Pokerogue-App
  - Android: https://github.com/Admiral-Billy/PokeRogue-App-Android
  - 离线包 Release: https://github.com/Admiral-Billy/pokerogue/releases
- HarmonyOS 官方文档（OpenHarmony docs 镜像）
  - 请求拦截与自定义响应：`en/application-dev/web/web-resource-interception-request-mgmt.md`
  - 加载网页/本地页：`en/application-dev/web/web-page-loading-with-web-components.md`
  - Cookie 与存储：`en/application-dev/web/web-cookie-and-data-storage-mgmt.md`
  - `Web` 属性：`en/application-dev/reference/apis-arkweb/arkts-basic-components-web-attributes.md`（`domStorageAccess`/`fileAccess`/`databaseAccess`）
  - `Web` 事件：`.../arkts-basic-components-web-events.md`（`onInterceptRequest`）
  - `WebResourceResponse`：`.../arkts-basic-components-web-WebResourceResponse.md`（`setResponseData` 支持 fd/ArrayBuffer）
  - `WebSchemeHandler`：`.../arkts-apis-webview-WebSchemeHandler.md`
  - `zlib`：`en/application-dev/reference/apis-basic-services-kit/js-apis-zlib.md`（`decompressFile`）
  - `request`：`.../js-apis-request.md`（`downloadFile`）
- 官方示例：https://gitee.com/harmonyos_samples/web-interceptor

---

## 9. 下一步建议

1. **先做 P0**：用 DevEco Studio 建空工程，`Web` 加载 `https://pokerogue.net` 验证内核兼容性。
2. P0 通过后按 4.3–4.7 落地 P1 离线闭环（先用 302MB 的 `game_futaba_mod.zip` 验证）。
3. 同步确认 HAP 签名/安装路径（开发者账号、设备 UDID、纯净模式）。
