# PokeRogue 鸿蒙版 —— UI/交互设计

> 配套文档：`方案.md`（技术调研与实现方案）。本文只讲**产品与界面设计**。
> 决策基线：**默认竖屏**（方向选项后续再加）；资源包**默认原版**，Futaba mod 作为后续可选项；**无登录/支付**；权限只要**读写 + 通知**。

---

## 1. 产品定义

| 项 | 内容 |
| --- | --- |
| 目标用户 | 想玩 PokeRogue 的人（无需注册、无需联网对战） |
| 核心路径 | **进入 → 下载核心包（已下载可跳过）→ 游玩** |
| 登录 / 支付 | 不需要 |
| 通知 | 仅下载进度（页面内 + 系统通知） |
| 后台行为 | 游戏可保留在后台；切后台**停止背景音乐**，回前台恢复 |
| 数据 | 下载的核心包（可后续更新替换） |
| 权限 | 读写权限、通知权限 |

### 权限说明

| 权限 | 用途 | 是否需弹窗申请 |
| --- | --- | --- |
| 通知权限 | 下载进度 / 完成 / 失败通知 | ✅ 首次下载前申请 |
| 文件读写 | 读写应用私有沙箱 `filesDir/game` | ❌ 沙箱内免申请（系统默认具备） |

> 结论：实际**只需申请通知权限**；文件读写在本应用沙箱内进行，不需要运行时授权。符合「读写 + 通知」的预期。

---

## 2. 页面流程

```
启动 Splash ──检查资源包──► 首页 Home
                              │
        ┌─────────┬───────────┼───────────────┐
        │         │           │               │
   未下载态    已下载态     有更新态        下载中(进度内嵌首页)
        │         │           │
        ▼         ▼           ▼
    下载页 Download      开始游戏 ──► 游戏页 Game（全屏 Web）
        │                             │
        └──完成→通知→回首页            └──返回键/手势→首页
```

- 已下载时启动直接进「已下载态」，**跳过下载**。
- 下载可在后台继续，进度同步到系统通知。

---

## 3. 线框图（默认竖屏）

### 3.1 启动页 Splash（自动，约 1–2 秒）

```
┌────────────────────────┐
│                        │
│                        │
│          🎮            │
│                        │
│      PokeRogue         │
│      鸿蒙离线版          │
│                        │
│                        │
│    正在检查资源包…       │
│                        │
└────────────────────────┘
```

- 检查 `filesDir/game/index.html` 是否存在及版本号，自动跳转首页。

### 3.2 首页 Home —— 未下载态

```
┌────────────────────────┐
│  PokeRogue        ⚙    │
│                        │
│          🎮            │
│                        │
│  尚未安装游戏核心包      │
│  首次游玩需先下载        │
│                        │
│  ◉ 原版        531 MB   │
│  ○ Futaba mod  302 MB   │  ← 后续计划，首版可置灰/隐藏
│                        │
│  ┌──────────────────┐  │
│  │     下载核心包     │  │  ← 主按钮
│  └──────────────────┘  │
│                        │
│  下载中可切后台，完成后通知 │
└────────────────────────┘
```

### 3.3 首页 Home —— 下载中（进度内嵌）

```
┌────────────────────────┐
│  PokeRogue        ⚙    │
│                        │
│    正在下载核心包         │
│       game.zip          │
│                        │
│   ▓▓▓▓▓▓▓▓░░░░░░░░     │
│        45%              │
│   239 MB / 531 MB       │
│   速度 8.2 MB/s          │
│                        │
│      [ 取消下载 ]        │
│                        │
│  💡 可切后台，进度同步到通知 │
└────────────────────────┘
```

### 3.4 首页 Home —— 解压中

```
┌────────────────────────┐
│  PokeRogue        ⚙    │
│                        │
│    正在解压核心包…       │
│                        │
│   ▓▓▓▓▓▓▓▓▓▓▓░░░░     │
│      (解压进度)          │
│                        │
│  请勿关闭应用            │
└────────────────────────┘
```

### 3.5 首页 Home —— 已下载态

```
┌────────────────────────┐
│  PokeRogue        ⚙    │
│                        │
│          🎮            │
│                        │
│   ✅ 核心包已就绪        │
│      版本 release-e4e9b53│
│      原版 · 531 MB       │
│                        │
│  ┌──────────────────┐  │
│  │    🎮 开始游戏    │  │  ← 主按钮（大）
│  └──────────────────┘  │
│                        │
│  [ 检查更新 ] [ 删除数据 ]│  ← 次要操作
└────────────────────────┘
```

### 3.6 首页 Home —— 有更新（叠加提示）

```
│  ┌──────────────────┐  │
│  │ 🆕 发现新版本      │  │
│  │ 当前 v1 → 最新 v2  │  │
│  │ [立即更新] [暂不]   │  │
│  └──────────────────┘  │
```

- 更新 = 重新下载新 zip → 解压覆盖 → 更新版本号；**不清 localStorage**，存档保留。

### 3.7 游戏页 Game（全屏 Web）

```
┌────────────────────────┐
│ ████████████████████████│
│ ███                     ███│
│ ███   游戏画面(Web组件)   ███│
│ ███   #touchControls    ███│
│ ███                     ███│
│ ████████████████████████│
│   [返回：边缘手势 / 悬浮小球]│
└────────────────────────┘
```

- 沉浸式，无覆盖 UI。
- 返回：屏幕边缘手势，或右上角半透明悬浮返回小球（点两次确认退出，防误触）。

### 3.8 设置页（从首页 ⚙ 进入）

```
┌────────────────────────┐
│  ← 设置                 │
│                        │
│  资源包来源   原版 ▾     │
│  当前版本     release-xxx│
│  存储占用     531 MB     │
│  屏幕方向     竖屏 ▾     │  ← 后续：跟随系统/横屏锁定
│                        │
│  [ 检查更新 ]            │
│  [ 删除本地数据 ]         │
│                        │
│  关于 / 来源声明          │
└────────────────────────┘
```

---

## 4. 资源包来源

- 首版**默认「原版」`game.zip`（531 MB）**。
- 「Futaba mod」（`game_futaba_mod.zip`，约 302 MB）列为**后续计划**，首版可隐藏或置灰。
- 数据结构预留「资源包来源列表」，便于后续扩展（原版 / Futaba mod / Kanto 等）。

---

## 5. 通知设计

| 类型 | 时机 | 样式 | 点击行为 |
| --- | --- | --- | --- |
| 进度通知 | 下载中（常驻，持续更新） | 标题 `PokeRogue 正在下载` + 进度条 + `45% · 239/531 MB` | 回到应用 |
| 完成通知 | 下载+解压完成 | `✅ 核心包下载完成，点击开始游戏` | 回到应用首页 |
| 失败通知 | 下载/解压失败 | `❌ 下载失败，点击重试` | 回到应用下载页 |

```
┌──────────────────────┐
│ PokeRogue   正在下载  │
│ ▓▓▓▓▓▓░░░░░░  45%    │
│ 239 MB / 531 MB      │
└──────────────────────┘
```

---

## 6. 后台与方向策略

| 场景 | 行为 |
| --- | --- |
| 下载中切后台 | 继续下载（后台任务），通知栏持续更新进度 |
| 游戏中切后台 | 保留游戏页面/进程，**暂停背景音乐**（不 kill） |
| 回到前台 | 恢复游戏画面，**恢复背景音乐** |
| 屏幕方向 | **默认竖屏**；设置页预留「跟随系统 / 横屏锁定」选项（后续实现） |

---

## 7. 验收标准对照

| 验收标准 | 对应设计 | 判定 |
| --- | --- | --- |
| 可打开 app | 启动页 → 首页正常渲染，状态判断正确 | 无崩溃、2 秒内出首页 |
| 可下载核心包，页面/通知看到进度 | 首页下载态进度条 + 系统进度通知，二者同步 | 进度单调递增、通知实时更新 |
| 下载完成后可游玩 | 解压成功 → 首页「已下载」→「开始游戏」→ 游戏页可玩、可存档 | 断网可玩、重启存档仍在 |

---

## 8. 页面 → 实现映射（供开发对照）

| 页面 | ArkUI 实现 | 关键能力 |
| --- | --- | --- |
| Splash | `Index.ets` 启动态 | 文件检查、路由跳转 |
| Home | `pages/Index.ets` | 三态切换、`Progress`、`Button` |
| Download | Home 内嵌 / 独立 `pages/Download.ets` | `request.downloadFile` + 进度回调 |
| Game | `pages/GamePage.ets` | `Web` + `onInterceptRequest` + `domStorageAccess` |
| 设置 | `pages/Settings.ets`（后续） | 资源来源、方向、删除数据 |
| 通知 | `NotificationManager` | 进度型通知 + `requestEnableNotification` |
